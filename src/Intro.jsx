import { useEffect, useState } from "react";

export default function Intro() {
    return null; // Intro disabled as per request

    return (
        <div className={`intro ${isFloating ? 'floating' : ''}`}>
            <span>spontan.</span>
        </div>
    );
}
